@page
@model SementesApplication.Pages.TeamModel.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Team</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Team.TeamID" />
            <div class="form-group">
                <label asp-for="Team.VolunteerID" class="control-label"></label>
                <input asp-for="Team.VolunteerID" class="form-control" />
                <span asp-validation-for="Team.VolunteerID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Team.ActionID" class="control-label"></label>
                <select asp-for="Team.ActionID" class="form-control" asp-items="ViewBag.ActionID"></select>
                <span asp-validation-for="Team.ActionID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
