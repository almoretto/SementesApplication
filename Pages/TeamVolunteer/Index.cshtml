@page
@model SementesApplication.IndexTeamVolunteer

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TeamVolunteer[0].Volunteer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TeamVolunteer[0].Team)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TeamVolunteer) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Volunteer.VolunteerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Team.TeamId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TeamVolunteerId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TeamVolunteerId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TeamVolunteerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
