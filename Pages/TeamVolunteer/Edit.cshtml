@page
@model SementesApplication.EditTeamVolunteer

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TeamVolunteer</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TeamVolunteer.TeamVolunteerId" />
            <div class="form-group">
                <label asp-for="TeamVolunteer.VolunteerId" class="control-label"></label>
                <select asp-for="TeamVolunteer.VolunteerId" class="form-control" asp-items="ViewBag.VolunteerId"></select>
                <span asp-validation-for="TeamVolunteer.VolunteerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TeamVolunteer.TeamId" class="control-label"></label>
                <select asp-for="TeamVolunteer.TeamId" class="form-control" asp-items="ViewBag.TeamId"></select>
                <span asp-validation-for="TeamVolunteer.TeamId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
